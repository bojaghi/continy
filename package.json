{
  "name": "bojaghi-continy",
  "scripts": {
    "wp-env": "wp-env",
    "wp-env:cli": "wp-env run cli --env-cwd=wp-content/continy",
    "composer": "npm run wp-env:cli composer",
    "wp": "npm run wp-env:cli wp",
    "test": "npm run wp-env:cli vendor/bin/phpunit"
  },
  "devDependencies": {
    "@wordpress/env": "^11.15.0"
  }
}
